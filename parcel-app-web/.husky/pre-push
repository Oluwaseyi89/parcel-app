#!/usr/bin/env sh
# Mirrors CI build gate before push.

cd "$(git rev-parse --show-toplevel)/parcel-app-web"

echo "-> [pre-push] Running production build..."
npm run build || { echo "x Build failed. Push aborted."; exit 1; }

echo "OK [pre-push] Build passed."
