import RouteStagePlaceholder from "@/components/RouteStagePlaceholder";

export default function VendorPage() {
  return (
    <RouteStagePlaceholder
      title="Vendor Portal"
      description="Vendor onboarding and login flow will be ported in Stage 4 with full API wiring and forms."
    />
  );
}
